{
  "name": "scp",
  "main": "./lib/scp",
  "version": "0.0.0",
  "private": true,
  "description": "Add directories to scp on file writes.",
  "activationEvents": ["scp:remoteWrite"],
  "repository": "https://github.com/mduvall/scp",
  "license": "MIT",
  "engines": {
    "atom": ">0.50.0"
  },
  "dependencies": {
    "underscore-plus": "1.x"
  }
}
